# Backlog

Possible work only. Backlog presence does not authorize execution. Promote selected work to `state/active/` with owner, branch, root SHA, write paths, dependencies, completion condition, and verification plan.
