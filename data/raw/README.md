# Raw Data

Provenance-preserving inputs. Raw does not mean authoritative or correct. Preserve source identity and acquisition context; normalize into `data/derived/` rather than mutating raw inputs in place.
