# Source

Canonical project implementation belongs here as new application/library code emerges. Existing bootstrap `autoresearch.py` and `sim/` remain in place until a separately planned migration; do not move them merely for cosmetic topology.
