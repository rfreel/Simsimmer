# Benchmarks

Benchmarks measure; they do not define correctness unless an experiment contract explicitly makes a threshold an acceptance criterion. Record environment, version/commit, dataset, repetitions, uncertainty, and whether the result is synthetic or empirical.
